# Changelog

## Runtime 0.3.0 — 2026-09-09

- 新增具名离线账户检查与维护 SDK/CLI，支持同一 actor ID 的密码/TOTP 更换与 MFA 重新引导、角色调整、普通停用/启用、目标全部设备退出。
- 账户版本检查与凭据/目标会话/幂等结果/审计原子提交；秘密不进入审计、缓存或无效 JSON 的诊断。TOTP 等价编码不能重置重放计数。
- auth schema 升级到 2，增加永久撤销登记并保守迁移旧停用账户；正常升级保留活跃账户，旧备份恢复要求当前撤销清单并强制完整凭据重新引导。
- 新增指定未兑换邀请取消，保留其他设备和邀请；独立固定包消费测试覆盖账户轮换、旧凭据失效和恢复后重新引导。

## Runtime 0.2.0 — 2026-09-09

- 新增可选参与者邀请与会话模块：单次兑换、期限、设备退出、永久撤销、撤回联动和恢复对账；凭据仅保存哈希。
- 按操作开放明确配置的邀请 self 身份，编辑发布与全部管理权限仍要求 MFA；资格来自服务端审核。
- 新增无需解密的本人状态摘要，覆盖未关联公开内容的记录，过滤所有权、同意、资格、到期与撤回。
- 新增受限匿名隐私报告 DTO、持久化限流、高熵状态回执及加密幂等重试；清理、撤回和恢复同步使相关回执失效。
- 自定义身份提供方支持事务内退出、主体撤销、导入失效与恢复；标准 CLI 可装载提供方，缺失或失败钩子拒绝操作并回滚。
- 独立消费示例提供 JSON 配置与参与者页面；运行时版本更新为 0.2.0，原有模块 schema 仍为 1。

## Runtime 0.1.1 — 2026-09-09

- 修复 link-only 来源无法保留到期语义：支持严格白名单 `data.rights.expiresAt`，到期即阻断发布及所有公开读取。
- 修复私有工作流导入可夹带未批准公开结果：复用配置语义校验，非法快照原子拒绝，保留合法导入与备份恢复。
- `lifecyclePublicResults` 要求当前 config，重新校验并从配置生成结果；缺配置或不匹配时不输出。HTTP 已接入，SDK 调用方需补传配置。
- 保持运行时/模块 schema 1；新到期字段需要 runtime 0.1.1+ 读取。

## Runtime 0.1.0 — 2026-09-09

- 新增固定版本 tgz 分发的可选受治理 runtime 和 CLI，独立消费目录无需复制平台服务源码。
- 新增不可变实体修订、固定版本证据、配置式领域/发布规则和统一公开投影。
- 提供具名密码 + TOTP MFA、会话撤销、配置权限、事务审核、审计与幂等结果。
- 提供加密私有工作流、同意与撤回、保留清理、旧 AAD 迁移和需要撤回对账的受控恢复。
- 提供版本化 SQLite 扩展迁移、可复用界面和消费方页面入口；受治理构建仅输出无内容页面。
- Graph v1 / core 0.2.0 的轻量和静态使用方式保持兼容。

## 0.2.0 — 2026-09-09

- 同仓库支持模板 → Pages 自动发布，以及同配置启动 Docker 服务器。
- 新增 `community.config.json` 与 `content/`，分离社区配置、用户内容和引擎源码。
- 新增拒绝覆盖已有数据库的初始化命令，并验证静态 → SQLite → 静态往返迁移。
- 独立分发 `@information-community/core`，包含 TypeScript 类型、包文档与 MIT 许可证；暂未发布 registry。
- 校验 JSON 扩展字段，忽略对象键顺序造成的虚假差异。
- 在真实校园指南答案详情页接入同主题答案导航。

## 0.1.0 — 2026-09-09

- 首个可运行版本：图契约、提案、静态草稿、HTTP / SQLite、领域示例和参考界面。
